data:
  name: "elec"
  preprocessing:
    deduplication:
      keep: "last"
    binarization:
      rating:
        threshold: 5
    split:
      train_ratio: 0.8
      valid_ratio: 0.1
      test_ratio: 0.1

seed: 42
